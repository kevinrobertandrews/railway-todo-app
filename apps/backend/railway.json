{
  "build": {
    "command": "npm install -g pnpm && pnpm install --frozen-lockfile --workspace-root && pnpm run build"
  },
  "start": {
    "command": "node dist/index.js"
  },
  "env": {
    "NODE_VERSION": "18"
  }
}
